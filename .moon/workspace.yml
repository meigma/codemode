# Shared Moon workspace configuration for CodeMode.

projects:
  globs: []
  sources:
    root: '.'
    docs: 'docs'

defaultProject: 'root'

vcs:
  defaultBranch: 'master'
  provider: 'github'

pipeline:
  installDependencies: false
