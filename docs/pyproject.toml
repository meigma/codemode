[project]
name = "codemode-docs"
version = "0.0.0"
description = "Reference documentation for the CodeMode Go library."
requires-python = ">=3.14"
dependencies = [
    "mkdocs-material>=9.7.0",
]

[tool.uv]
package = false
